<?xml version="1.0" encoding="utf-8"?>
<!--
  Everything the SDK needs to run is declared here so it merges into the app
  automatically. Without this an integrator gets a crash at the first download
  with nothing pointing at the cause.

  RECORD_AUDIO is deliberately NOT declared: it is a runtime permission the app
  must request and justify to the user, so it belongs in the app's manifest
  where a reviewer will see it.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Model downloads run in a foreground service so a 491 MB transfer survives
         the app being backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Capture keeps running while the app is backgrounded. Without a
         microphone-typed foreground service Android silently stops delivering
         audio, which reads as the assistant having gone deaf. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application>
        <service
            android:name="com.s2s.mobile.model.ModelDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name="com.s2s.mobile.audio.VoiceSessionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>

</manifest>
